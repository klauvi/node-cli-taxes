# Taxes and Totals exercise

```
Usage: node-cli-taxes [options]

Options:
  -h, --help            show help
  --zipcode <code>      Zipcode for tax calculation
  --subtotal <amount>   Amount to be calcluated
```
